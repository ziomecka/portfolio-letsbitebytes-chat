const Box = (): unknown => {
  return {
    MuiBox: {
      root: {
        maxHeight: '100%',
        maxWidth: '100%',
        height: '100%',
        width: '100%',
      },
    },
  };
};

export { Box };
